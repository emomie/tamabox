---
phase: 05-design-system-foundation
plan: 05
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - webroot/css/cake.css
  - webroot/css/fonts.css
  - webroot/css/home.css
autonomous: false

requirements: [DS-01]

must_haves:
  truths:
    - "Legacy CSS files (cake.css, fonts.css, home.css) are deleted if unreferenced, OR explicitly retained with documented usage if referenced"
    - "`composer test` passes (all existing tests green — no regressions)"
    - "`composer phpcs` and `composer phpstan` pass if present in composer.json (CSS changes should not affect PHP analysis, but verify)"
    - "Smoke verification confirms: tokens.css, colors_and_type.css load via 200 OK; Calm Gacha turquoise visible on existing buttons; Noto Sans JP renders; no console errors; no layout regressions on the 7 existing live pages"
  artifacts:
    - path: ".planning/phases/05-design-system-foundation/05-05-SUMMARY.md"
      provides: "Phase 5 final verification record + cleanup decision documentation"
      contains: "composer test"
  key_links:
    - from: "verification"
      to: "all prior plans (05-01..05-04)"
      via: "smoke test on live tamabox.emomie.com or local PHP server"
      pattern: "tokens.css|colors_and_type.css|tamabox.css"
---

<objective>
Final-mile verification of Phase 5:
1. Investigate whether the legacy CSS files `webroot/css/{cake.css, fonts.css, home.css}` are still referenced anywhere in the codebase. If unreferenced, delete them. If referenced, leave a note for Phase 6 to handle.
2. Run the full automated test suite (`composer test`) and static analysis to confirm zero regressions.
3. Perform a smoke verification (manual or automated) that the new CSS chain loads correctly and existing pages render with Calm Gacha values.
4. Produce the Phase 5 closing summary.

This is the only plan with `autonomous: false` — Task 3 is a `checkpoint:human-verify` because the only definitive proof that Calm Gacha values render in a browser requires either visual confirmation or a deployed-page screenshot, which is outside what Claude can autonomously assert.

Output: Cleanup decisions committed, test results captured, smoke verification approved by user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/claude/projects/tamabox/.planning/STATE.md
@/home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-CONTEXT.md
@/home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@/home/claude/projects/tamabox/composer.json

<interfaces>
<!-- Legacy CSS files to investigate (CONTEXT.md Area 4 line 41):
     webroot/css/cake.css (3596 bytes, dated 2026-04-22)
     webroot/css/fonts.css (2896 bytes, dated 2026-04-22)
     webroot/css/home.css (1523 bytes, dated 2026-04-22)
     These were scaffolded by CakePHP `bake` but tamabox does not appear to reference them
     (preliminary grep during planning showed no hits in templates/ or src/). -->

<!-- Existing Phase 4 verification baseline (MANUAL-SMOKE-CHECKLIST.md): the 12-item smoke
     test that was used to ship v1. Re-using its structure for Phase 5 smoke. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate legacy CSS file usage and remove unreferenced files</name>
  <files>webroot/css/cake.css, webroot/css/fonts.css, webroot/css/home.css</files>
  <read_first>
    - /home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-CONTEXT.md (section "Area 4 — 移行戦略 / 破壊範囲" line 41: "webroot/css/{cake,fonts,home}.css は Phase 5 内で grep して使用調査、参照ゼロなら削除タスクを含める")
  </read_first>
  <action>
    Run the following investigation commands from the tamabox project root (`/home/claude/projects/tamabox`):

    ```
    grep -rn "cake\.css\|fonts\.css\|home\.css" templates/ src/ config/ webroot/ 2>/dev/null
    grep -rn "'cake'\|'fonts'\|'home'" templates/ src/ 2>/dev/null | grep -i "Html->css\|HtmlHelper"
    ```

    Interpret results:

    **Case A — No references found (expected outcome based on planning-stage grep)**:
    Delete the three files:
    ```
    rm webroot/css/cake.css
    rm webroot/css/fonts.css
    rm webroot/css/home.css
    ```
    Document the deletion in the task summary.

    **Case B — Any reference found (unexpected)**:
    DO NOT delete the referenced files. Instead, document in the SUMMARY:
    - Which file is referenced
    - What template / source file references it
    - Recommendation to handle in Phase 6 (which template will be Calm-Gacha-migrated and may remove the dependency)
    Leave the referenced file(s) in place; delete only the unreferenced ones.

    **Case C — Mixed (some referenced, some not)**:
    Delete only the unreferenced ones. Document the remaining files for Phase 6.

    For each deleted file, also confirm via `git status` (if this were a tracked repo) or `ls webroot/css/` that the file is gone.

    Note: tamabox does have CakePHP's `cake.css` file from the initial bake (these are CakePHP framework default styles that decorate the welcome / error / debug pages). If any of these are referenced ONLY by CakePHP error / debug templates in `vendor/cakephp/cakephp/templates/` (not in our `templates/`), they ARE deletable — those CakePHP default templates fall back gracefully if the CSS is missing, and the live tamabox app doesn't render those welcome pages. The grep above scopes to `templates/ src/ config/ webroot/` (NOT vendor/), which is correct.
  </action>
  <verify>
    <automated>cd /home/claude/projects/tamabox &amp;&amp; { grep -rn "cake\.css\|fonts\.css\|home\.css" templates/ src/ config/ webroot/ 2>/dev/null | wc -l; } &gt; /tmp/legacy_css_refs.txt &amp;&amp; cat /tmp/legacy_css_refs.txt &amp;&amp; ls webroot/css/ &amp;&amp; echo INVESTIGATED</automated>
  </verify>
  <acceptance_criteria>
    - Grep investigation has run; output is captured in the task summary
    - If grep returned 0 results: `webroot/css/cake.css`, `webroot/css/fonts.css`, `webroot/css/home.css` no longer exist
    - If grep returned references: only the unreferenced subset has been deleted; the referenced files remain with a deferral note
    - `ls webroot/css/` now shows the post-Phase-5 CSS file inventory (at minimum: normalize.min.css, milligram.min.css, tokens.css, colors_and_type.css, tamabox.css)
    - Action taken is documented (which files deleted, which retained, with reason)
  </acceptance_criteria>
  <done>Cleanup investigation complete with a documented disposition for each of the 3 legacy CSS files. Unreferenced files deleted; referenced files retained with a note for Phase 6.</done>
</task>

<task type="auto">
  <name>Task 2: Run composer test + static analysis to confirm zero regressions</name>
  <files>(none modified — verification only)</files>
  <read_first>
    - /home/claude/projects/tamabox/composer.json (to see which test/lint scripts are available)
  </read_first>
  <action>
    Run the existing CI gate commands from the tamabox project root in this order:

    ```
    cd /home/claude/projects/tamabox
    composer test
    ```

    The tamabox v1 baseline is 195 tests, 0 failures (per CONTEXT.md Area 4 line 42 and STATE.md). Phase 5 changes are CSS / template / static asset only, so the test count should remain 195 and all tests should pass.

    If `composer phpstan` is defined as a composer script (check with `composer run-script --list`), also run:
    ```
    composer phpstan
    ```

    If `composer phpcs` is defined, run:
    ```
    composer phpcs
    ```

    Capture exit codes and the final summary line (`OK (195 tests, N assertions)` for phpunit, `No errors` for phpstan, `No phpcs violations` for phpcs).

    If ANY of these fail:
    1. Read the failure output carefully — Phase 5 should not cause PHP test/lint regressions (the only PHP file touched is `templates/layout/default.php` in Plan 05-01 and `templates/element/icon.php` in Plan 05-02)
    2. Most likely cause if failure: `php -l` already verified syntax during Plans 05-01 and 05-02, so a logic error is unlikely. Check for: any test that asserts on the exact CSS chain in default.php (e.g., `assertCount(3, ...)` after counting css files), or any test that grep-matches the old `--color-accent: #0085FF` value
    3. Report the failure clearly and STOP — do not attempt blind fixes; await user direction in the checkpoint task that follows

    On success, record the exact final lines of `composer test` output (test count + assertion count) for the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/claude/projects/tamabox &amp;&amp; composer test 2>&amp;1 | tee /tmp/phase5_test.log &amp;&amp; tail -5 /tmp/phase5_test.log &amp;&amp; grep -qE "OK \([0-9]+ tests" /tmp/phase5_test.log &amp;&amp; echo TEST_OK</automated>
  </verify>
  <acceptance_criteria>
    - `composer test` exits 0
    - Final summary line of phpunit output matches pattern `OK (XXX tests, YYY assertions)` (regex: `OK \([0-9]+ tests, [0-9]+ assertions\)`)
    - Test count is greater than or equal to v1 baseline (195) — Phase 5 adds no tests but adds no regressions; equality (195) is expected
    - If `composer phpstan` is defined: it exits 0 with `[OK] No errors` or equivalent
    - If `composer phpcs` is defined: it exits 0
    - Test log captured to `/tmp/phase5_test.log` (visible in task output)
  </acceptance_criteria>
  <done>Automated test suite green, static analysis green (if available). Phase 5 introduced zero regressions to PHP code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke verification — visual confirmation of Calm Gacha values + page render integrity</name>
  <what-built>
    Phase 5 (Plans 05-01 through 05-04 + cleanup in 05-05 Task 1):
    - tokens.css + colors_and_type.css copied verbatim from handoff_tamabox
    - Google Fonts (Noto Sans JP 400/700 + JetBrains Mono 500) wired into layout/default.php
    - tamabox.css :root rewritten with Calm Gacha aliases — all `--color-*`/`--radius-*`/`--shadow-subtle`/`--font-family` now resolve to `--tb-*` tokens
    - .tb-btn (5 variants: primary, ghost, quiet, disabled, danger) + .tb-icon-btn defined
    - .tb-card (with 18px padding + shadow-1), .tb-card-soft, .tb-letter (honey left border) defined
    - .tb-chip svg icon sizing helper added (base in tokens.css)
    - .tb-input states (.is-over-limit / --over-limit / :disabled) added
    - .tb-unread-dot added inside .tb-tabbar__item
    - .tb-appbar / __title / __sub / --big / --transparent defined
    - 13 icon SVGs at webroot/img/icons/ + templates/element/icon.php inline helper
    - Legacy CSS files cleaned up per investigation
    - composer test green
  </what-built>
  <how-to-verify>
    Pick ONE of two verification paths:

    **Path A — Local PHP built-in server (no deploy needed)**:

    1. Start a temporary PHP server from the tamabox project root:
       ```
       cd /home/claude/projects/tamabox
       php -S 127.0.0.1:8765 -t webroot/ &
       ```
       (Note: this may not run controllers without CakePHP routing, but should serve static CSS/SVG correctly. If it does not render PHP routes, skip to Path B.)
    2. `curl -s http://127.0.0.1:8765/css/tokens.css | head -20` — should show the verbatim tokens.css header comment "tamabox · hi-fi design tokens"
    3. `curl -s http://127.0.0.1:8765/css/colors_and_type.css | head -5` — should NOT contain `@import` and should contain `--accent: var(--tb-turq-400)`
    4. `curl -s http://127.0.0.1:8765/img/icons/inbox.svg` — should return SVG markup with `viewBox="0 0 24 24"` and `stroke-width="1.6"`
    5. `curl -s http://127.0.0.1:8765/img/icons/bell.svg` — should contain `M6 16V11a6 6 0 0 1 12 0v5l2 2H4z`
    6. Stop the server: `kill %1` (or find the PID and kill)

    **Path B — Live site (tamabox.emomie.com)**:

    1. Confirm the changes have been deployed (per project convention — typically `git push` triggers Xserver lolipop pull, OR explicit `rsync`/`scp` per project deploy convention). If deploy has not happened: ask the user to deploy first, then resume.
    2. Open https://tamabox.emomie.com/ in a browser
    3. Open DevTools → Network tab → reload page
    4. Verify the CSS request order in Network: `normalize.min.css`, `milligram.min.css`, `tokens.css`, `colors_and_type.css`, `tamabox.css` — all return 200
    5. Verify the font CDN requests: at least one `fonts.gstatic.com` request for Noto Sans JP returns 200
    6. DevTools → Elements → pick the `<html>` or `<body>` → Styles panel → "computed" → verify:
       - `--tb-turq-400` resolves to `#2FA597`
       - `--tb-font-sans` includes `Noto Sans JP`
       - `--color-accent` resolves to `var(--tb-turq-400)` → `#2FA597` (the alias chain works)
    7. Visual checks (the eyeball is the final arbiter for DS-06):
       - The "Bluesky でログイン" button (or similar primary CTA) is turquoise, not blue
       - The page background is `#F2F3F4` (slightly cooler than v1's `#F8F9FA`)
       - Body text rendered in Noto Sans JP (visible CJK glyph improvement over system fonts)
       - Button corners look softer (radius 6px / 10px instead of 4px / 8px)
    8. Navigate through the live pages and confirm none are broken: Home (/), Send form (/m/...), Settings, Block list, Report form, Account delete. Per UI-SPEC line 580-589 "No Regression" checklist.

    Either path: capture screenshots or paste curl output into the resume signal so the verification result is auditable.

    Failure modes to look for:
    - `tokens.css` 404 → file not deployed or path wrong (Plan 05-01 Task 1)
    - Buttons still blue → tamabox.css :root rewrite did not happen (Plan 05-03 Task 1)
    - Fonts not loading → Google Fonts `<link>` tag missing or malformed (Plan 05-01 Task 3)
    - Page layout broken (overlapping elements, missing borders) → alias mismatch; check `var(--tb-line)` resolves to a color string, not `unset`
  </how-to-verify>
  <resume-signal>
    Reply "approved" (and optionally paste a screenshot or curl output snippet) if:
    - All CSS files load with 200
    - Calm Gacha turquoise is visible on existing buttons
    - No layout regressions on the 7 live tamabox pages

    Reply with a description of any issue observed if not approved (e.g., "tokens.css 404", "buttons still blue", "Send form layout broken at line 3"), and Phase 5 will be revised before closing.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None added. Verification + cleanup task only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering (accidental deletion of referenced asset) | Task 1 legacy CSS cleanup | mitigate | Cleanup is conditional: only delete files with zero references in templates/src/config/webroot. Grep results captured for audit. |
| T-05-07 | Information Disclosure (test output may include paths or stack traces) | Task 2 composer test log | accept | Test log is local-only (/tmp/phase5_test.log), not transmitted. No PII in test fixtures. |

No new attack surface — verification + cleanup only.
</threat_model>

<verification>
- Task 1 grep investigation captured and disposition documented
- Task 2 `composer test` exits 0 with `OK (195+ tests, ... assertions)` summary
- Task 3 human-verify checkpoint approved with evidence (screenshot, curl output, or explicit "approved" + page-by-page confirmation)
</verification>

<success_criteria>
- All 6 DS requirements (DS-01 through DS-06) are observably satisfied in the live or local environment:
  - DS-01: tokens.css loads via Html->css chain ✓
  - DS-02: Noto Sans JP + JetBrains Mono served from Google Fonts ✓
  - DS-03: .tb-btn 5 variants present in stylesheet (verifiable via DevTools class search) ✓
  - DS-04: .tb-card / .tb-card-soft / .tb-letter present ✓
  - DS-05: .tb-chip / .tb-input / .tb-tabbar / .tb-appbar present ✓
  - DS-06: existing pages visually switched from blue to turquoise; --color-* aliases resolve to --tb-* ✓
- composer test green (195+ tests, 0 failures)
- Legacy CSS cleanup investigation has a documented outcome
- No regressions on the 7 live tamabox v1 pages
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system-foundation/05-05-SUMMARY.md` documenting:
- Cleanup investigation outcome (which files deleted, which retained, grep evidence)
- composer test output summary (final OK line)
- composer phpstan / phpcs outcome if available
- Smoke verification path used (Path A or Path B), evidence collected
- Final webroot/css/ file inventory
- Cumulative line count change in tamabox.css (911 → ~1100)
- Explicit list of all 6 DS-* requirements with "✓ satisfied" or "✗ unmet" status and pointer to artifact location
</output>
