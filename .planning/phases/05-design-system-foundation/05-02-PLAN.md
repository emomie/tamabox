---
phase: 05-design-system-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webroot/img/icons/inbox.svg
  - webroot/img/icons/send.svg
  - webroot/img/icons/user.svg
  - webroot/img/icons/bell.svg
  - webroot/img/icons/compass.svg
  - webroot/img/icons/back.svg
  - webroot/img/icons/close.svg
  - webroot/img/icons/more.svg
  - webroot/img/icons/check.svg
  - webroot/img/icons/chevron.svg
  - webroot/img/icons/letter.svg
  - webroot/img/icons/star.svg
  - webroot/img/icons/heart.svg
  - templates/element/icon.php
autonomous: true
requirements: [DS-05]

must_haves:
  truths:
    - "13 SVG icon files exist at webroot/img/icons/ with verbatim path data from handoff_tamabox/components.jsx"
    - "All SVG files share viewBox 0 0 24 24, stroke 1.6, stroke-linecap round, stroke-linejoin round, fill none, stroke currentColor"
    - "`templates/element/icon.php` exists and accepts $name and $size parameters and returns inline SVG output for the same 13 icon names"
  artifacts:
    - path: "webroot/img/icons/inbox.svg"
      provides: "Inbox icon for tab bar item 1"
      contains: "M3 12l3-7h12l3 7"
    - path: "webroot/img/icons/send.svg"
      provides: "Send icon"
      contains: "M21 3 11 13"
    - path: "webroot/img/icons/bell.svg"
      provides: "Bell icon for notifications tab"
      contains: "M6 16V11a6 6 0 0 1 12 0v5l2 2H4z"
    - path: "templates/element/icon.php"
      provides: "Inline SVG helper accepting $name and $size"
      contains: "switch"
  key_links:
    - from: "templates/element/icon.php"
      to: "(13 icon names mapped to SVG markup)"
      via: "switch/match on $name"
      pattern: "case 'inbox'"
---

<objective>
Deliver the full 13-icon SVG set in BOTH delivery formats (static files at `webroot/img/icons/*.svg` AND an inline helper element at `templates/element/icon.php`). Per UI-SPEC line 429-433, both forms are required: static files for `<img src>` / CSS `background-image` usage, and the helper for templates where `currentColor` inheritance is needed.

Purpose: Phase 6/7/8 consume these icons in tab bars, app bars, message rows, settings lists, reveal animations, etc. Without them, no hi-fi screen can render. This plan runs in parallel with 05-01 (no file overlap) to maximize Wave 1 throughput.

Output:
- 13 SVG files at `webroot/img/icons/{inbox,send,user,bell,compass,back,close,more,check,chevron,letter,star,heart}.svg`
- `templates/element/icon.php` PHP helper
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-CONTEXT.md
@/home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@/home/claude/projects/handoff_tamabox/components.jsx

<interfaces>
<!-- SVG wrapper attribute set (verbatim from UI-SPEC lines 422-427) -->
All 13 SVGs share these wrapper attributes:
```
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" width="24" height="24">
  <!-- inner path/circle/rect elements -->
</svg>
```

<!-- Inner element data (verbatim from components.jsx lines 27-39, mirrored in UI-SPEC lines 408-420) -->
inbox:    <path d="M3 12l3-7h12l3 7" /><path d="M3 12v6a1 1 0 0 0 1 1h16a1 1 0 0 0 1-1v-6" /><path d="M3 12h5l1 2h6l1-2h5" />
send:     <path d="M21 3 11 13" /><path d="M21 3 14 21l-3-8-8-3z" />
user:     <circle cx="12" cy="8" r="4" /><path d="M4 21c1.5-4 4.5-6 8-6s6.5 2 8 6" />
bell:     <path d="M6 16V11a6 6 0 0 1 12 0v5l2 2H4z" /><path d="M10 20a2 2 0 0 0 4 0" />
compass:  <circle cx="12" cy="12" r="9" /><path d="m15 9-2 5-5 2 2-5z" />
back:     <path d="m15 6-6 6 6 6" />
close:    <path d="M6 6l12 12M6 18 18 6" />
more:     <circle cx="5" cy="12" r="1.4" fill="currentColor" stroke="none"/><circle cx="12" cy="12" r="1.4" fill="currentColor" stroke="none"/><circle cx="19" cy="12" r="1.4" fill="currentColor" stroke="none"/>
check:    <path d="M5 12.5 10 17l9-10" />
chevron:  <path d="m9 6 6 6-6 6" />
letter:   <rect x="3" y="6" width="18" height="13" rx="2" /><path d="m4 8 8 6 8-6" />
star:     <path d="M12 3.5 14.5 9l6 .7-4.5 4 1.3 6L12 16.8 6.7 19.7l1.3-6L3.5 9.7 9.5 9z" />
heart:    <path d="M12 20s-7-4.5-9-9a5 5 0 0 1 9-3 5 5 0 0 1 9 3c-2 4.5-9 9-9 9z" />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webroot/img/icons/ directory and write 13 SVG files</name>
  <files>
    webroot/img/icons/inbox.svg, webroot/img/icons/send.svg, webroot/img/icons/user.svg, webroot/img/icons/bell.svg, webroot/img/icons/compass.svg, webroot/img/icons/back.svg, webroot/img/icons/close.svg, webroot/img/icons/more.svg, webroot/img/icons/check.svg, webroot/img/icons/chevron.svg, webroot/img/icons/letter.svg, webroot/img/icons/star.svg, webroot/img/icons/heart.svg
  </files>
  <read_first>
    - /home/claude/projects/handoff_tamabox/components.jsx (lines 27-39 — IconInbox through IconHeart definitions, verbatim source)
    - /home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-UI-SPEC.md (section "Icon Set Contract" lines 386-433)
  </read_first>
  <action>
    Create the directory `webroot/img/icons/` (precedent: `webroot/img/default-avatar.svg` exists, so SVG files in `img/` are an established pattern).

    Then create exactly 13 SVG files. Each file is a complete standalone SVG with this skeleton:

    ```
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" width="24" height="24">
      [INNER_ELEMENTS]
    </svg>
    ```

    Substitute `[INNER_ELEMENTS]` with the verbatim path/circle/rect data from the `<interfaces>` block above (which is itself verbatim from `handoff_tamabox/components.jsx` lines 27-39).

    Exact file mapping (the JSX `IconChev` becomes `chevron.svg` — note the rename to match UI-SPEC line 400 / 417):

    | File | Source const | Inner elements |
    |------|--------------|----------------|
    | inbox.svg | IconInbox | 3 paths (lid + body + slats) |
    | send.svg | IconSend | 2 paths (paper-plane fold + body) |
    | user.svg | IconUser | 1 circle (head) + 1 path (shoulders) |
    | bell.svg | IconBell | 1 path (bell body) + 1 path (clapper) |
    | compass.svg | IconCompass | 1 circle (rim) + 1 path (needle) |
    | back.svg | IconBack | 1 path (left chevron) |
    | close.svg | IconClose | 1 path (× double-line) |
    | more.svg | IconMore | 3 circles with `fill="currentColor" stroke="none"` |
    | check.svg | IconCheck | 1 path (checkmark) |
    | chevron.svg | IconChev | 1 path (right chevron) |
    | letter.svg | IconLetter | 1 rect + 1 path (envelope flap) |
    | star.svg | IconStar | 1 path (5-point star) |
    | heart.svg | IconHeart | 1 path (heart curve) |

    For `more.svg`, the 3 circles retain their inline `fill="currentColor" stroke="none"` attributes — this overrides the outer svg's `fill="none" stroke="currentColor"`. Do NOT alter those inline attributes.

    Indent inner elements with 2 spaces for readability:
    ```
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" width="24" height="24">
      <path d="M3 12l3-7h12l3 7" />
      <path d="M3 12v6a1 1 0 0 0 1 1h16a1 1 0 0 0 1-1v-6" />
      <path d="M3 12h5l1 2h6l1-2h5" />
    </svg>
    ```

    Use the Write tool for each file (do NOT use heredoc / `cat << EOF`).
  </action>
  <verify>
    <automated>cd /home/claude/projects/tamabox/webroot/img/icons &amp;&amp; test -f inbox.svg &amp;&amp; test -f send.svg &amp;&amp; test -f user.svg &amp;&amp; test -f bell.svg &amp;&amp; test -f compass.svg &amp;&amp; test -f back.svg &amp;&amp; test -f close.svg &amp;&amp; test -f more.svg &amp;&amp; test -f check.svg &amp;&amp; test -f chevron.svg &amp;&amp; test -f letter.svg &amp;&amp; test -f star.svg &amp;&amp; test -f heart.svg &amp;&amp; grep -lF 'M3 12l3-7h12l3 7' inbox.svg &amp;&amp; grep -lF 'M21 3 11 13' send.svg &amp;&amp; grep -lF 'M6 16V11a6 6 0 0 1 12 0v5l2 2H4z' bell.svg &amp;&amp; grep -lF 'fill="currentColor" stroke="none"' more.svg &amp;&amp; for f in *.svg; do grep -q 'stroke-width="1.6"' "$f" || { echo "FAIL: $f missing stroke-width"; exit 1; }; done &amp;&amp; for f in *.svg; do grep -q 'viewBox="0 0 24 24"' "$f" || { echo "FAIL: $f missing viewBox"; exit 1; }; done &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Directory `webroot/img/icons/` exists
    - All 13 SVG files exist with the exact filenames listed above
    - Every SVG contains `viewBox="0 0 24 24"`
    - Every SVG contains `stroke-width="1.6"`
    - Every SVG contains `stroke-linecap="round"` and `stroke-linejoin="round"`
    - Every SVG contains `fill="none"` AND `stroke="currentColor"` on the outer `<svg>` tag
    - `inbox.svg` contains all 3 path d-values: `M3 12l3-7h12l3 7`, `M3 12v6a1 1 0 0 0 1 1h16a1 1 0 0 0 1-1v-6`, `M3 12h5l1 2h6l1-2h5`
    - `bell.svg` contains `M6 16V11a6 6 0 0 1 12 0v5l2 2H4z` and `M10 20a2 2 0 0 0 4 0`
    - `user.svg` contains `<circle cx="12" cy="8" r="4"` and `M4 21c1.5-4 4.5-6 8-6s6.5 2 8 6`
    - `compass.svg` contains `<circle cx="12" cy="12" r="9"` and `m15 9-2 5-5 2 2-5z`
    - `more.svg` contains 3 occurrences of `fill="currentColor" stroke="none"` (one per dot)
    - `letter.svg` contains `<rect x="3" y="6" width="18" height="13" rx="2"`
    - `star.svg` contains `M12 3.5 14.5 9l6 .7-4.5 4 1.3 6L12 16.8 6.7 19.7l1.3-6L3.5 9.7 9.5 9z`
    - `chevron.svg` (file name; source const is IconChev) contains `m9 6 6 6-6 6`
  </acceptance_criteria>
  <done>All 13 SVG icon files exist with verbatim path data from components.jsx, sharing the same wrapper attributes per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Create templates/element/icon.php inline SVG helper</name>
  <files>templates/element/icon.php</files>
  <read_first>
    - /home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-UI-SPEC.md (lines 429-433 — "Provide BOTH: 1. Static SVG files. 2. Inline SVG helper at templates/element/icon.php accepting $name and $size parameters")
    - /home/claude/projects/tamabox/templates/element/avatar_handle_chip.php (read briefly to confirm CakePHP element conventions in this codebase — variable handling, escaping, etc.)
    - /home/claude/projects/handoff_tamabox/components.jsx (lines 27-39 for verbatim path data)
  </read_first>
  <action>
    Create `templates/element/icon.php` — a CakePHP element that accepts two parameters via `$this->element('icon', ['name' => 'inbox', 'size' => 24])`:

    - `$name` (string, required) — one of: `inbox`, `send`, `user`, `bell`, `compass`, `back`, `close`, `more`, `check`, `chevron`, `letter`, `star`, `heart`
    - `$size` (int, optional, default 24) — width/height in pixels

    The element MUST output an inline `<svg>` element (NOT an `<img src>` reference — UI-SPEC line 432 explicitly justifies the inline helper as the path that supports `currentColor` inheritance). The output shape is identical to the standalone files, but the `width` and `height` attributes use the `$size` parameter.

    Use this exact structure:

    ```php
    <?php
    /**
     * Inline SVG icon helper.
     *
     * Usage: <?= $this->element('icon', ['name' => 'inbox', 'size' => 24]) ?>
     *
     * @var string $name  One of: inbox, send, user, bell, compass, back, close, more, check, chevron, letter, star, heart
     * @var int    $size  Width/height in pixels (default 24)
     *
     * SVG path data verbatim from handoff_tamabox/components.jsx (Phase 5).
     */
    $size = isset($size) ? (int)$size : 24;
    $inner = match ($name ?? '') {
        'inbox'   => '<path d="M3 12l3-7h12l3 7" /><path d="M3 12v6a1 1 0 0 0 1 1h16a1 1 0 0 0 1-1v-6" /><path d="M3 12h5l1 2h6l1-2h5" />',
        'send'    => '<path d="M21 3 11 13" /><path d="M21 3 14 21l-3-8-8-3z" />',
        'user'    => '<circle cx="12" cy="8" r="4" /><path d="M4 21c1.5-4 4.5-6 8-6s6.5 2 8 6" />',
        'bell'    => '<path d="M6 16V11a6 6 0 0 1 12 0v5l2 2H4z" /><path d="M10 20a2 2 0 0 0 4 0" />',
        'compass' => '<circle cx="12" cy="12" r="9" /><path d="m15 9-2 5-5 2 2-5z" />',
        'back'    => '<path d="m15 6-6 6 6 6" />',
        'close'   => '<path d="M6 6l12 12M6 18 18 6" />',
        'more'    => '<circle cx="5" cy="12" r="1.4" fill="currentColor" stroke="none"/><circle cx="12" cy="12" r="1.4" fill="currentColor" stroke="none"/><circle cx="19" cy="12" r="1.4" fill="currentColor" stroke="none"/>',
        'check'   => '<path d="M5 12.5 10 17l9-10" />',
        'chevron' => '<path d="m9 6 6 6-6 6" />',
        'letter'  => '<rect x="3" y="6" width="18" height="13" rx="2" /><path d="m4 8 8 6 8-6" />',
        'star'    => '<path d="M12 3.5 14.5 9l6 .7-4.5 4 1.3 6L12 16.8 6.7 19.7l1.3-6L3.5 9.7 9.5 9z" />',
        'heart'   => '<path d="M12 20s-7-4.5-9-9a5 5 0 0 1 9-3 5 5 0 0 1 9 3c-2 4.5-9 9-9 9z" />',
        default   => '',
    };
    ?>
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" width="<?= $size ?>" height="<?= $size ?>" aria-hidden="true"><?= $inner ?></svg>
    ```

    Notes:
    - `aria-hidden="true"` is added because decorative icon usage is the default; templates that use icons semantically (e.g., a standalone icon button) supply their own `aria-label` on the parent `<button>` per UI-SPEC line 381.
    - The `match` expression requires PHP 8.0+ — confirmed available (project runs PHP 8.3.6 per VPS env memory).
    - Inner SVG markup is inlined as a string and rendered with `<?= $inner ?>` — this is safe because the strings are hardcoded constants in this file, not user input. Do NOT pipe them through `h()` (which would escape `<` and break the SVG).
    - Unknown `$name` falls through to `default => ''`, producing an empty `<svg>` (renders nothing visible) instead of throwing — defensive default.
  </action>
  <verify>
    <automated>test -f /home/claude/projects/tamabox/templates/element/icon.php &amp;&amp; cd /home/claude/projects/tamabox &amp;&amp; php -l templates/element/icon.php &amp;&amp; grep -q "match (\$name" templates/element/icon.php &amp;&amp; grep -q "'inbox'" templates/element/icon.php &amp;&amp; grep -q "'heart'" templates/element/icon.php &amp;&amp; grep -q 'M21 3 11 13' templates/element/icon.php &amp;&amp; grep -q 'stroke-width="1.6"' templates/element/icon.php &amp;&amp; grep -q 'viewBox="0 0 24 24"' templates/element/icon.php &amp;&amp; grep -q 'aria-hidden="true"' templates/element/icon.php &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `templates/element/icon.php` exists
    - `php -l templates/element/icon.php` exits 0 (no syntax errors)
    - File contains `match ($name`
    - File contains all 13 case strings: `'inbox'`, `'send'`, `'user'`, `'bell'`, `'compass'`, `'back'`, `'close'`, `'more'`, `'check'`, `'chevron'`, `'letter'`, `'star'`, `'heart'`
    - File contains string `M3 12l3-7h12l3 7` (inbox path verbatim)
    - File contains string `M12 20s-7-4.5-9-9` (heart path verbatim)
    - File contains `viewBox="0 0 24 24"`, `stroke-width="1.6"`, `aria-hidden="true"`
    - File contains the `default => ''` fallback case
    - File contains `$size = isset($size) ? (int)$size : 24;` (or equivalent default initialization)
  </acceptance_criteria>
  <done>Icon helper element exists with PHP 8 match expression mapping all 13 icon names to verbatim SVG path data; renders inline SVG with currentColor support and parameterized size.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template author ↔ icon.php | New element exposes `$name` parameter — already-trusted (template code, not user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Injection (XSS via SVG) | templates/element/icon.php | mitigate | `$name` is matched against a closed set via PHP `match` — unknown values produce empty output, never echoed. Inner SVG strings are hardcoded literals, never user-derived. No `h()` escaping needed because there is no user data path. |

No new attack surface beyond static asset addition. Icon SVG files are served by CakePHP's static webroot.
</threat_model>

<verification>
- Both task automated checks pass
- `find webroot/img/icons -name '*.svg' | wc -l` returns 13
- `php -l templates/element/icon.php` exits 0
- After plan completes, Phase 6 templates can call `<?= $this->element('icon', ['name' => 'inbox']) ?>` and have currentColor SVG render
</verification>

<success_criteria>
- All 13 SVG files exist at `webroot/img/icons/` with verbatim path data from components.jsx
- All SVGs share the standard wrapper (viewBox 0 0 24 24, stroke 1.6, currentColor, round caps/joins)
- `templates/element/icon.php` resolves any of the 13 names to inline SVG
- Both delivery formats satisfied per UI-SPEC lines 429-433 (BOTH static files AND inline helper)
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system-foundation/05-02-SUMMARY.md` documenting:
- 13 SVG files created (list with file sizes)
- icon.php helper signature: `$this->element('icon', ['name' => string, 'size' => int])`
- Note that `aria-hidden="true"` is the default — semantic icon-button usage requires parent `aria-label`
</output>
