---
phase: 05-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webroot/css/tokens.css
  - webroot/css/colors_and_type.css
  - templates/layout/default.php
autonomous: true
requirements: [DS-01, DS-02]

must_haves:
  truths:
    - "`webroot/css/tokens.css` exists with verbatim contents from handoff_tamabox/tokens.css"
    - "`webroot/css/colors_and_type.css` exists with the `@import url('tokens.css');` line removed"
    - "Google Fonts `<link>` tags for Noto Sans JP (400, 700) + JetBrains Mono (500) are present in `templates/layout/default.php` before the Html->css() call"
    - "`templates/layout/default.php` Html->css array loads order is exactly: normalize.min, milligram.min, tokens, colors_and_type, tamabox"
  artifacts:
    - path: "webroot/css/tokens.css"
      provides: "Calm Gacha primitive tokens (--tb-* variables and utility classes)"
      contains: "--tb-turq-400"
    - path: "webroot/css/colors_and_type.css"
      provides: "Semantic alias layer (--fg-*, --bg-*, --accent, --type-*)"
      contains: "--accent: var(--tb-turq-400)"
    - path: "templates/layout/default.php"
      provides: "CSS chain + Google Fonts link tags"
      contains: "fonts.googleapis.com"
  key_links:
    - from: "templates/layout/default.php"
      to: "webroot/css/tokens.css"
      via: "Html->css(['normalize.min', 'milligram.min', 'tokens', 'colors_and_type', 'tamabox'])"
      pattern: "'tokens'"
    - from: "templates/layout/default.php"
      to: "Google Fonts CDN"
      via: "<link href=\"https://fonts.googleapis.com/css2?...\""
      pattern: "fonts.googleapis.com"
---

<objective>
Establish the token layer (tokens.css + colors_and_type.css) and font loading infrastructure that all subsequent Phase 5 plans (and Phase 6+) consume.

Purpose: Without these files in `webroot/css/` and Noto Sans JP / JetBrains Mono loaded by the browser, no `--tb-*` variable resolves and the alias rewrite in Plan 05-03 produces nothing visible. This plan is the foundation.

Output:
- `webroot/css/tokens.css` (verbatim from handoff_tamabox)
- `webroot/css/colors_and_type.css` (verbatim minus the `@import` line)
- Layout file updated with Google Fonts preconnect/link tags and updated Html->css() chain order
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/claude/projects/tamabox/.planning/STATE.md
@/home/claude/projects/tamabox/.planning/ROADMAP.md
@/home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-CONTEXT.md
@/home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@/home/claude/projects/handoff_tamabox/tokens.css
@/home/claude/projects/handoff_tamabox/colors_and_type.css
@/home/claude/projects/tamabox/templates/layout/default.php

<interfaces>
<!-- Existing Html->css() call to be modified, line 16 of templates/layout/default.php -->
Current:
```php
<?= $this->Html->css(['normalize.min', 'milligram.min', 'tamabox']) ?>
```
Target:
```php
<?= $this->Html->css(['normalize.min', 'milligram.min', 'tokens', 'colors_and_type', 'tamabox']) ?>
```

<!-- Google Fonts link tag to be inserted BEFORE the Html->css() line (after `<?= $this->Html->meta('icon') ?>`) -->
Insert verbatim:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@500&family=Noto+Sans+JP:wght@400;700&display=swap" rel="stylesheet">
```
Note: UI-SPEC line 59 is the source of truth for this exact URL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy tokens.css verbatim to webroot/css/</name>
  <files>webroot/css/tokens.css</files>
  <read_first>
    - /home/claude/projects/handoff_tamabox/tokens.css (full file, 259 lines — this is the verbatim source)
    - /home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-UI-SPEC.md (section "Token File Contract" line 32-50)
  </read_first>
  <action>
    Create `webroot/css/tokens.css` as an EXACT BYTE-FOR-BYTE copy of `~/projects/handoff_tamabox/tokens.css`. Do not modify, reformat, reorder, or rewrite any content. Use `cp` to be safe:

    ```
    cp /home/claude/projects/handoff_tamabox/tokens.css /home/claude/projects/tamabox/webroot/css/tokens.css
    ```

    The file MUST contain all 259 lines including:
    - `:root` block with all `--tb-paper`, `--tb-paper-deep`, `--tb-card`, `--tb-card-soft`, `--tb-line`, `--tb-line-strong` surface tokens
    - All ink tokens `--tb-ink` through `--tb-ink-4`
    - Full turquoise palette `--tb-turq-50` through `--tb-turq-ink`
    - Warm/honey palette `--tb-warm-100` through `--tb-warm-700`
    - Signal tokens `--tb-danger`, `--tb-danger-bg`
    - Font stacks `--tb-font-sans`, `--tb-font-serif`, `--tb-font-mono`
    - Radius tokens `--tb-r-sm` (6px), `--tb-r-md` (10px), `--tb-r-lg` (14px), `--tb-r-xl` (20px), `--tb-r-pill` (999px)
    - Shadow tokens `--tb-shadow-1`, `--tb-shadow-2`, `--tb-shadow-3`
    - All utility classes including `.tb`, `.tb-mono`, `.tb-phone*`, `.tb-col`, `.tb-row`, `.tb-spacer`, `.tb-btn` (base + primary/ghost/quiet/full), `.tb-card`, `.tb-chip` (default/warm/quiet), `.tb-hr`, `.tb-tabbar` (with __item + is-active), `.tb-input` (with focus + placeholder + .tb-label), `.tb-paper-grain`

    Note: This file contains BOTH the `:root` tokens AND a subset of utility class CSS already. Plan 05-04 will add additional components that aren't in this file (e.g., `.tb-letter`, `.tb-card-soft` rules, `.tb-appbar*`, `.tb-btn--disabled`, `.tb-btn--danger`, `.tb-icon-btn`) to `tamabox.css` — but the duplicate rules already in tokens.css must remain untouched (cascade will let `tamabox.css` win if any redefinition happens later).

    Do NOT add a Phase 5 comment header — keep the file pristine for git diff clarity against handoff_tamabox.
  </action>
  <verify>
    <automated>diff -q /home/claude/projects/handoff_tamabox/tokens.css /home/claude/projects/tamabox/webroot/css/tokens.css &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `/home/claude/projects/tamabox/webroot/css/tokens.css` exists
    - `diff` between handoff source and destination returns no differences (exit 0)
    - File contains string `--tb-turq-400:   #2FA597;` (verifiable with `grep -F "--tb-turq-400:   #2FA597;" webroot/css/tokens.css`)
    - File contains string `--tb-font-sans: 'Noto Sans JP'` (verifiable with grep)
    - File contains string `.tb-btn--primary` (verifiable with grep)
    - `wc -l webroot/css/tokens.css` returns 259
  </acceptance_criteria>
  <done>tokens.css is byte-identical to handoff_tamabox/tokens.css and resides at webroot/css/tokens.css.</done>
</task>

<task type="auto">
  <name>Task 2: Copy colors_and_type.css with @import line stripped</name>
  <files>webroot/css/colors_and_type.css</files>
  <read_first>
    - /home/claude/projects/handoff_tamabox/colors_and_type.css (78 lines, source)
    - /home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-UI-SPEC.md (lines 38-50, especially the rationale for removing the @import)
  </read_first>
  <action>
    Create `webroot/css/colors_and_type.css` as a copy of `~/projects/handoff_tamabox/colors_and_type.css` with ONE modification: remove line 5 (the `@import url('tokens.css');` statement). The rationale (UI-SPEC line 50): tokens.css is loaded separately by CakePHP's Html->css() chain, so the @import is redundant and would cause an extra HTTP request to a relative path that may 404 (CakePHP serves CSS from /css/, not relative to the importing file).

    Use this sequence:
    ```
    cp /home/claude/projects/handoff_tamabox/colors_and_type.css /home/claude/projects/tamabox/webroot/css/colors_and_type.css
    sed -i "/@import url('tokens.css');/d" /home/claude/projects/tamabox/webroot/css/colors_and_type.css
    ```

    After modification the file MUST contain (verbatim from handoff):
    - File-level comment block (lines 1-3 of source, slightly shifted)
    - `:root { ... }` block with:
      - Semantic foreground `--fg-1` through `--fg-4`, `--fg-accent`, `--fg-honey`, `--fg-danger`
      - Semantic background `--bg-page`, `--bg-deep`, `--bg-card`, `--bg-soft`, `--bg-honey`, `--bg-danger`
      - Lines `--line`, `--line-strong`
      - Accents `--accent`, `--accent-hover`, `--accent-press`, `--honey`
      - Type roles `--type-display`, `--type-h1`, `--type-h2`, `--type-h3`, `--type-body`, `--type-body-lg`, `--type-meta`, `--type-mono`, `--type-label`
      - Tracking `--tracking-ja`, `--tracking-en`, `--tracking-label`, `--tracking-mono`
      - Elevation `--elev-1`, `--elev-2`, `--elev-3`
      - Radii `--r-sm`, `--r-md`, `--r-lg`, `--r-xl`, `--r-pill`
    - Semantic helper selectors `h1, .h1`, `h2, .h2`, `h3, .h3`, `p, .body`, `.body-lg`, `.meta`, `.mono`, `.label-en`

    The only difference from the source MUST be the deletion of the single `@import url('tokens.css');` line.
  </action>
  <verify>
    <automated>test -f /home/claude/projects/tamabox/webroot/css/colors_and_type.css &amp;&amp; ! grep -q "@import" /home/claude/projects/tamabox/webroot/css/colors_and_type.css &amp;&amp; grep -q -- "--accent: var(--tb-turq-400)" /home/claude/projects/tamabox/webroot/css/colors_and_type.css &amp;&amp; grep -q -- "--type-mono:    500 12px/1.5 var(--tb-font-mono)" /home/claude/projects/tamabox/webroot/css/colors_and_type.css &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `webroot/css/colors_and_type.css` exists
    - File does NOT contain the string `@import`
    - File contains string `--accent:        var(--tb-turq-400);`
    - File contains string `--type-h1:      700 22px/1.5 var(--tb-font-sans);`
    - File contains string `--type-mono:    500 12px/1.5 var(--tb-font-mono);`
    - File contains string `--fg-honey:  var(--tb-warm-700);`
    - File contains string `.label-en` selector
    - Line count is 77 (78 source - 1 removed @import line)
  </acceptance_criteria>
  <done>colors_and_type.css is byte-equivalent to source minus the @import line, and resides at webroot/css/colors_and_type.css.</done>
</task>

<task type="auto">
  <name>Task 3: Update layout/default.php with Google Fonts links and extended CSS chain</name>
  <files>templates/layout/default.php</files>
  <read_first>
    - /home/claude/projects/tamabox/templates/layout/default.php (current 46 lines)
    - /home/claude/projects/tamabox/.planning/phases/05-design-system-foundation/05-UI-SPEC.md (section "Google Fonts `<link>` Tag" lines 53-60, and "CSS Load Order" lines 44-50)
  </read_first>
  <action>
    Modify `templates/layout/default.php` with TWO edits:

    **Edit A — Insert Google Fonts link tags after line 15 (`<?= $this->Html->meta('icon') ?>`) and BEFORE line 16 (the Html->css() call):**

    Insert the following three lines verbatim (preserve the 4-space indentation matching surrounding `<?=` tags):

    ```
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@500&family=Noto+Sans+JP:wght@400;700&display=swap" rel="stylesheet">
    ```

    The URL is exactly per UI-SPEC line 59: family names URL-encoded as `JetBrains+Mono:wght@500` and `Noto+Sans+JP:wght@400;700`, with `display=swap`. Note: per UI-SPEC line 26 the font weights loaded are Noto Sans JP (400, 700) + JetBrains Mono (500). Use that exact URL — do NOT add weights 500/600 to Noto Sans JP even though Phase 5 type roles use weight 600 for `.tb-chip` / `.tb-btn` — Google Fonts synthesizes intermediate weights from 400/700 axis when subsets are not loaded, and UI-SPEC explicitly chose this minimal set.

    **Edit B — Replace line 16 (the existing Html->css call):**

    Change:
    ```php
        <?= $this->Html->css(['normalize.min', 'milligram.min', 'tamabox']) ?>
    ```
    to:
    ```php
        <?= $this->Html->css(['normalize.min', 'milligram.min', 'tokens', 'colors_and_type', 'tamabox']) ?>
    ```

    Order is load-critical (per UI-SPEC line 50 rationale): normalize → milligram → tokens → colors_and_type → tamabox.

    Do NOT modify any other line in the file (header element, body, identity check, flash render, etc. remain untouched).
  </action>
  <verify>
    <automated>grep -q "fonts.googleapis.com/css2?family=JetBrains+Mono:wght@500&amp;family=Noto+Sans+JP:wght@400;700&amp;display=swap" /home/claude/projects/tamabox/templates/layout/default.php &amp;&amp; grep -q "Html->css(\['normalize.min', 'milligram.min', 'tokens', 'colors_and_type', 'tamabox'\])" /home/claude/projects/tamabox/templates/layout/default.php &amp;&amp; cd /home/claude/projects/tamabox &amp;&amp; php -l templates/layout/default.php &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `templates/layout/default.php` contains string `fonts.googleapis.com/css2?family=JetBrains+Mono`
    - File contains string `rel="preconnect" href="https://fonts.googleapis.com"`
    - File contains string `rel="preconnect" href="https://fonts.gstatic.com" crossorigin`
    - File contains string `Html->css(['normalize.min', 'milligram.min', 'tokens', 'colors_and_type', 'tamabox'])`
    - File does NOT contain the old chain `Html->css(['normalize.min', 'milligram.min', 'tamabox'])` (the 3-item form)
    - `php -l templates/layout/default.php` reports `No syntax errors detected`
  </acceptance_criteria>
  <done>Layout loads tokens + colors_and_type CSS in the correct order and the Google Fonts CDN link tags are present before the css() call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Google Fonts CDN | New cross-origin font CSS fetch added via `<link>` tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Google Fonts CDN link | accept | Standard practice; the `preconnect crossorigin` hint follows MDN recommendation. No PII transmitted — only Referer header (origin only). |
| T-05-02 | Denial of Service (font CDN downtime) | `--tb-font-sans` resolution | mitigate | Token stack falls back to `system-ui, -apple-system, 'Hiragino Kaku Gothic ProN', sans-serif` if Google Fonts unreachable; `display=swap` prevents FOIT. |

No new authn/authz boundaries, no new user input handling, no new server-side data flows. Phase is CSS / static asset / `<link>` additions only.
</threat_model>

<verification>
- All 3 task automated checks pass
- `php -l templates/layout/default.php` exits 0
- After plan completes, hitting any existing tamabox page via curl should return HTML with the new `<link>` tags and 5-item CSS chain visible in the head
</verification>

<success_criteria>
- `webroot/css/tokens.css` is byte-identical to `handoff_tamabox/tokens.css` (259 lines)
- `webroot/css/colors_and_type.css` is the source minus the single `@import` line (77 lines)
- `templates/layout/default.php` loads Google Fonts and the 5-file CSS chain in the order: normalize.min → milligram.min → tokens → colors_and_type → tamabox
- No PHP syntax errors
- No existing functionality affected (existing `--color-*` vars in tamabox.css still resolve because tamabox.css is still loaded last in chain; the alias rewrite happens in Plan 05-03)
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system-foundation/05-01-SUMMARY.md` documenting:
- Files added (tokens.css size, colors_and_type.css size after @import strip)
- Layout file line range modified
- Confirmation that no other files were touched
</output>
