---
phase: 06-v1-calm-gacha
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - templates/Pages/home.php
  - webroot/css/tamabox.css
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Home page renders the Calm Gacha hero (✦ + display title + lead) per Home.jsx"
    - "Three-step HOW list renders with mono badges, in JSX order"
    - "Bluesky login CTA still POSTs to /auth/start-bluesky with CSRF token"
    - "No legacy classes (`.home-page`, `.display-heading`, `.home-lead`, `.primary-button`) referenced from this template"
    - "composer test still 195+ tests / 0 failures"
  artifacts:
    - path: "templates/Pages/home.php"
      provides: "Calm Gacha Home / landing page"
      contains: "tb-btn--primary"
---

<objective>
Convert Home page to Calm Gacha layout per `Home.jsx`. Hero block with ✦ symbol, display title, lead copy, "HOW" divider, 3-step list with mono number badges, and a primary CTA "Bluesky でログイン" (kept verbatim from existing copy for behavior parity).
</objective>

<context>
- /home/claude/projects/tamabox/.planning/phases/06-v1-calm-gacha/06-UI-SPEC.md (Screen 2)
- /home/claude/projects/handoff_tamabox/screens/Home.jsx (89 lines)
- /home/claude/projects/tamabox/templates/Pages/home.php (current 31 lines)
- /home/claude/projects/tamabox/webroot/css/tokens.css (tb-paper-grain, tb-mono, tb-btn--primary)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite home.php with Calm Gacha hero + HOW + steps + CTA</name>
  <files>templates/Pages/home.php</files>
  <action>
Replace the entire template. Keep the `$this->assign('title', 'ホーム')` line.

The new template renders (top to bottom):
1. `.tb-home` wrapper (background `--tb-paper`, padding from JSX `36px 28px 24px`)
2. `.tb-paper-grain` decorative background element
3. Hero block — centered ✦ + h1 + lead paragraph
4. Divider with "HOW" label
5. `<ol class="tb-step-list">` with 3 `<li>` items (numbers 01-03, titles from JSX)
6. CTA: `Form->create` with `class="tb-home__cta-form"` and a primary `.tb-btn--primary.tb-btn--full` button (Bluesky icon SVG inline — reuse the BlueskyMark from Home.jsx as inline SVG)

Append CSS to tamabox.css under the Phase 6 section:

```css
/* §G.2 — Home page (UI-01) */
.tb-home {
    background: var(--tb-paper);
    padding: 36px 28px 24px;
    min-height: calc(100vh - 56px);  /* below existing .header-bar */
    box-sizing: border-box;
    position: relative;
    display: flex;
    flex-direction: column;
}
.tb-home__hero {
    text-align: center;
    padding-top: 32px;
}
.tb-home__symbol {
    font-size: 64px;
    line-height: 1;
    color: var(--tb-warm-500);
    font-weight: 300;
    letter-spacing: 0.06em;
}
.tb-home__title {
    font-size: 30px;
    font-weight: 700;
    margin: 22px 0 0;
    letter-spacing: 0.22em;
    color: var(--tb-ink);
}
.tb-home__lead {
    margin: 14px 0 0;
    font-size: 13.5px;
    color: var(--tb-ink-2);
    line-height: 1.85;
    letter-spacing: 0.06em;
    font-weight: 400;
}
.tb-home__divider {
    display: flex;
    align-items: center;
    gap: 12px;
    margin: 38px 0 28px;
}
.tb-home__divider-line {
    flex: 1;
    height: 1px;
    background: var(--tb-line);
}
.tb-home__divider-label {
    font-size: 10px;
    font-weight: 600;
    color: var(--tb-ink-3);
    letter-spacing: 0.32em;
}
.tb-step-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 18px;
}
.tb-step-list__item {
    display: flex;
    gap: 14px;
    align-items: flex-start;
}
.tb-step-list__num {
    font-size: 11px;
    font-weight: 500;
    color: var(--tb-warm-700);
    border: 1px solid var(--tb-warm-300);
    border-radius: 999px;
    padding: 4px 9px;
    letter-spacing: 0.1em;
    flex: 0 0 auto;
    background: var(--tb-warm-100);
    font-family: var(--tb-font-mono);
}
.tb-step-list__title {
    font-size: 15px;
    font-weight: 600;
    color: var(--tb-ink);
}
.tb-step-list__sub {
    font-size: 12px;
    color: var(--tb-ink-3);
    margin-top: 2px;
    letter-spacing: 0.04em;
}
.tb-home__cta {
    margin-top: 32px;
    display: flex;
    flex-direction: column;
    gap: 10px;
}
.tb-home__cta-form { margin: 0; }
.tb-home__bluesky-mark {
    width: 18px;
    height: 18px;
    flex: 0 0 auto;
}
```

Note: keep `$this->assign('title', 'ホーム')` so layout title still works. The form helper still emits CSRF token.

Use this PHP template structure:

```php
<?php
/**
 * @var \App\View\AppView $this
 *
 * UI-01 — Home / landing (Phase 6 Calm Gacha rewrite of UI-SPEC v1 §4).
 * Hi-fi: ~/projects/handoff_tamabox/screens/Home.jsx
 */
$this->assign('title', 'ホーム');
?>
<div class="tb-home">
    <div class="tb-paper-grain" aria-hidden="true"></div>

    <section class="tb-home__hero">
        <div class="tb-home__symbol" aria-hidden="true">✦</div>
        <h1 class="tb-home__title">tamabox</h1>
        <p class="tb-home__lead">
            送信者が出るかもしれない、<br>匿名メッセージ箱。
        </p>
    </section>

    <div class="tb-home__divider" aria-hidden="true">
        <span class="tb-home__divider-line"></span>
        <span class="tb-home__divider-label">HOW</span>
        <span class="tb-home__divider-line"></span>
    </div>

    <ol class="tb-step-list">
        <li class="tb-step-list__item">
            <span class="tb-step-list__num">01</span>
            <div>
                <div class="tb-step-list__title">受信箱をつくる</div>
                <div class="tb-step-list__sub">Bluesky でログイン</div>
            </div>
        </li>
        <li class="tb-step-list__item">
            <span class="tb-step-list__num">02</span>
            <div>
                <div class="tb-step-list__title">URL をシェアする</div>
                <div class="tb-step-list__sub">誰でも匿名でメッセージを送れる</div>
            </div>
        </li>
        <li class="tb-step-list__item">
            <span class="tb-step-list__num">03</span>
            <div>
                <div class="tb-step-list__title">稀に、送信者が現れる</div>
                <div class="tb-step-list__sub">SSR 確率で身元が開示されます <span style="color: var(--tb-warm-500);">✦</span></div>
            </div>
        </li>
    </ol>

    <div class="tb-home__cta">
        <?= $this->Form->create(null, [
            'url' => ['controller' => 'Auth', 'action' => 'startBluesky'],
            'type' => 'post',
            'class' => 'tb-home__cta-form',
        ]) ?>
            <button type="submit" class="tb-btn tb-btn--primary tb-btn--full">
                <svg class="tb-home__bluesky-mark" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                    <path d="M5.8 4.5c2.6 1.9 5.4 5.8 6.2 8 .8-2.2 3.6-6.1 6.2-8 1.9-1.4 4.8-2.4 4.8 1 0 .7-.4 5.6-.6 6.4-.7 2.8-3.5 3.5-6 3-4.4-.7-5.5 1.6-3.1 3.9 4.5 4.4 6.4 9.9 3.7 12.6-2.7 2.6-5.6 1.8-7-1.7-.5-1.2-.7-1.8-.9-3.9 0 0 0 0 0 0s-.4 2.7-.9 3.9c-1.4 3.5-4.3 4.3-7 1.7-2.7-2.7-.8-8.2 3.7-12.6 2.4-2.3 1.3-4.6-3.1-3.9-2.5.5-5.3-.2-6-3-.2-.8-.6-5.7-.6-6.4 0-3.4 2.9-2.4 4.8-1z"/>
                </svg>
                <span>Bluesky でログイン</span>
            </button>
        <?= $this->Form->end() ?>
    </div>
</div>
```

Note copy: keep "Bluesky でログイン" (existing v1 copy) rather than hi-fi's "Bluesky ではじめる" — the existing copy matches the production page button text the user expects.
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; php -l templates/Pages/home.php &amp;&amp; grep -q "tb-btn--primary" templates/Pages/home.php &amp;&amp; grep -q "tb-step-list" templates/Pages/home.php &amp;&amp; ! grep -q "home-page\|primary-button\|display-heading\|home-lead" templates/Pages/home.php &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains `tb-btn--primary`, `tb-step-list`, `tb-home__hero`
    - File does NOT contain `home-page`, `display-heading`, `home-lead`, `primary-button`
    - Form->create still posts to `Auth::startBluesky`
    - php -l clean
  </acceptance_criteria>
  <done>Home page renders the Calm Gacha hero + steps + Bluesky CTA per Home.jsx.</done>
</task>

</tasks>

<verifier>

## (a) Hi-fi side-by-side check
- Open `~/projects/handoff_tamabox/screens/Home.jsx` and the new `templates/Pages/home.php` side by side.
- Confirm: ✦ glyph → "tamabox" title → 2-line lead → HOW divider → 3 numbered steps → CTA button.
- Confirm Bluesky SVG path matches the BlueskyMark from JSX lines 85-88.

## (b) composer test
Must end "OK", 195+ tests, 0 failures.

## (c) Controller behavior unchanged
- POST `/auth/start-bluesky` route still resolves
- CSRF token still embedded by Form->create
- `src/Controller/AuthController.php` not touched

## (d) MANUAL-SMOKE-CHECKLIST
- Visit `/` while logged out — Home page renders
- Click "Bluesky でログイン" — redirects to Bluesky OAuth start endpoint
- Page renders without horizontal scroll on a 390px-wide viewport

</verifier>

<risks>
- Existing `.home-page` / `.home-explainer` / `.display-heading` / `.primary-button` CSS in tamabox.css becomes orphaned but harmless (no other caller).
- The hi-fi uses 30px display title which is outside the locked typography scale (22 / 18 / 16 / 15 / 14 / 12 / 11 / 10). This is approved as Home is a one-off marketing-style display heading — flag in summary, do not block.
</risks>

<out_of_scope>
- Tab bar (Phase 7)
- Onboarding (v3 candidate ONB-01)
- Bluesky butterfly logo asset replacement (v3 ASSET-01)
</out_of_scope>
