---
phase: 8
plan: 08-03
slug: edge-02-inbox-closed
status: ready
depends_on: [08-01]
requirements: [EDGE-02]
created: 2026-05-13
---

# Plan 08-03 — EDGE-02: Send inbox-closed branch rewrite to hi-fi SendInboxClosed

## Goal

Replace the `!$isAccepting` else branch of `templates/Messages/send.php` (currently a 4-line `.tb-card-soft` block) with the hi-fi `SendInboxClosed` layout: warm status block (kicker dot + uppercase label + title + body) + disabled textarea stand-in + disabled CTA at the bottom.

## Tasks

1. Re-read `08-UI-SPEC.md` §"Component 3 — `Messages/send.php` else-branch rewrite (EDGE-02)" and hi-fi `SendErrors.jsx` lines 71-161.
2. Open `templates/Messages/send.php`:
   - Locate the `<?php if (!$isAccepting): ?>` block (lines 79-83 current).
   - Replace its body with the new `.tb-send__closed-status` block + disabled textarea stand-in per UI-SPEC.
   - Below the existing `<?php endif; ?>` for the textarea-block, add a new `<?php if (!$isAccepting): ?>` block that renders the disabled `.tb-send__closed-cta` button as the screen's footer CTA. Use `<button type="button" disabled>` so no form submit fires.
   - DO NOT touch the `<?php if ($isAccepting): ?>` form-create / form-end wrapping (CSRF safety).
3. Update existing test `tests/TestCase/Controller/MessagesControllerTest.php::testSendGetIsAcceptingFalseHidesForm`:
   - Change the assertion from `'現在この受信箱は受け付けていません'` to `'この箱はいま受信を停止しています'`.
   - Keep the `assertResponseNotContains('<textarea name="body"')` assertion (the disabled stand-in is a `<div>`, not a `<textarea>`).
   - Add a new assertion: `$this->assertResponseContains('inbox · paused');` (kicker label sanity check).
4. Run `composer test 2>&1 | tail -20` → must end "OK", 0 failures.

## Files

- `templates/Messages/send.php` (modify — else-branch rewrite + disabled CTA)
- `tests/TestCase/Controller/MessagesControllerTest.php` (modify — update existing test, +1 assertion)

## Verifier

- (a) Hi-fi side-by-side: visually match `~/projects/handoff_tamabox/screens/SendErrors.jsx` lines 71-161. Confirm the warm status block (kicker dot + uppercase label + title + body), the disabled textarea stand-in (paper-deep bg, cursor not-allowed, centered text), and the disabled paper-deep CTA at the bottom.
- (b) `composer test` green: 200 → 200 tests (no count delta), 0 failures.
- (c) Requirement satisfied: EDGE-02 — `is_accepting=false` inbox now renders the hi-fi SendInboxClosed copy + warm status block (not the v1 plain card-soft).

## Risks

- The receiver card (`.tb-send__receiver`) at the top still renders for the closed branch (matches hi-fi line 82-100). DO NOT hide it.
- Existing test substring `'現在この受信箱は受け付けていません'` is also asserted in `testSendPostToClosedInboxRedirectsWithError` (line 271 area) via Flash message — that test path is unaffected (Flash error string is in `MessagesController.php`, not the template). Verify by grep before changing template copy.
- Consent label block (`<?php if ($isAccepting): ?>` branch line 113) is correctly gated; closed branch should NOT show consent.

## Out of scope

- Removing the welcome-message section (it appears above the closed status — keep, matches hi-fi if welcome exists)
- Showing a "reopens at" timestamp (no schema field for that — v3 candidate)
- Modifying the controller (no controller change for EDGE-02)
