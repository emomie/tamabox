---
phase: 8
plan: 08-01
slug: phase-i-css-foundations
status: ready
depends_on: []
requirements: [MOTION-01]
created: 2026-05-13
---

# Plan 08-01 — Phase 8 §I CSS Section + MOTION-01

## Goal

Append the §I CSS section to `webroot/css/tamabox.css` (after §H.8). This is the foundation everything else in Phase 8 depends on: it provides the universal `.tb-btn:active` press-scale animation (MOTION-01), the 6 layout helper sub-sections (error screen scaffold, closed-inbox block, overflow live state, send-failed banner, block modal, Phase 7 cleanup helpers), and `prefers-reduced-motion` guards.

## Tasks

1. Re-read `08-UI-SPEC.md` §"CSS map — §I additions" and the 7 §I.* sub-sections (lines 460-720 of the UI-SPEC).
2. Open `webroot/css/tamabox.css` and append a new section after line 2413 (current EOF, after §H.8 .tb-dash-avatar).
3. Add the section banner comment:
   ```css
   /* ============================================================
    * Phase 8 — Edge cases + motion polish
    * Source: .planning/phases/08-edge-microinteractions/08-UI-SPEC.md
    * ============================================================ */
   ```
4. Add §I.1 through §I.7 exactly per UI-SPEC (use `var(--tb-*)` tokens; literal hex only for the 3 documented edge tones `#EFD5D2`, `#FFFBEF`, `#F0DCA8` already established in Phase 7).
5. Cross-reference `08-CONTEXT.md` Locked Decision — Phase 7 deferred color exception in a `/* #FBFCFD inherited — see 08-CONTEXT.md */` comment inside §I.7.
6. Run `composer test 2>&1 | tail -20` → must end "OK", 0 failures.

## Files

- `webroot/css/tamabox.css` (modify; append ~280 lines)

## Verifier

- (a) Hi-fi side-by-side: no direct hi-fi for foundations, but spot-check the rule selectors match the UI-SPEC class names exactly.
- (b) `composer test` green: tests do not assert on CSS, so this should be a no-op for PHPUnit. Confirm 199 tests / 0 failures.
- (c) Requirement satisfied: MOTION-01 (`.tb-btn:active scale(0.985) 80ms` works on all 5 variants because the rule sits below variant-specific `:active` overrides in cascade order — primary keeps its background change, others get only the transform).

## Risks

- Cascading conflict: `tokens.css` line 164 already has `.tb-btn--primary:active { transform: scale(0.985); ... }`. Our new generic `.tb-btn:active { transform: scale(0.985); }` doesn't conflict — both produce the same transform on primary; the primary `background: var(--tb-turq-500);` override is preserved.
- Literal hex colors triggering Dimension 3 lint: ensure each literal has an adjacent `/* edge tone of var(--tb-*) — single-use */` comment.

## Out of scope

- Touching `tokens.css` (no new CSS variables this plan)
- Modifying any PHP / JS file (those come in 08-02..08-07)
- Removing legacy CSS rules (deferred to v3)
