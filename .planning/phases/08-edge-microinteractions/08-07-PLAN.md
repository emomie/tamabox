---
phase: 8
plan: 08-07
slug: phase-7-cleanup
status: ready
depends_on: [08-01, 08-06]
requirements: [Phase-7-deferred-D-21, Phase-7-deferred-D-22, Phase-7-deferred-D-23, Phase-7-deferred-D-24]
created: 2026-05-13
---

# Plan 08-07 — Phase 7 deferred cleanup (inline-styles, controller view-data, locked decisions)

## Goal

Close all Phase 7 deferred carry-over items (D-21..D-24 from `08-CONTEXT.md`):
1. **D-21:** Eliminate the 17 inline `style="..."` attributes in `templates/Users/dashboard.php` by replacing them with §I.7 helper classes (or existing classes where applicable). Target: `grep -c 'style="' templates/Users/dashboard.php` → 0.
2. **D-22:** Drop the unused `$blocks` from `UsersController::dashboard()` `$this->set()` (template doesn't reference it).
3. **D-23:** Confirm `#FBFCFD` row-tint locked decision is documented in `08-CONTEXT.md` (already there per Phase 8 追記 section) and add a cross-reference comment near `tamabox.css:2136`.
4. **D-24:** Document the `margin-top: 3px` minor off-grid in `08-CONTEXT.md` as an "Acknowledged sub-grid micro-offset".

## Tasks

1. Re-read `08-UI-SPEC.md` §"Component 7 — Phase 7 cleanup (D-21..D-24)" — the table maps each inline style to its replacement class.
2. Open `templates/Users/dashboard.php` and apply the inline-style → class mapping per UI-SPEC table:
   - Line 68: `style="flex:1; min-width:0;"` → `class="tb-flex-grow"`
   - Line 76: outer `style="display:flex; align-items:baseline; gap:10px;"` → `class="tb-flex-row--baseline"`
   - Line 123: `style="flex:1; min-width:0;"` → `class="tb-flex-grow"`
   - Line 150 hit-card corner glyph `<span>` → `class="tb-reveal-hit-card__corner-glyph"`
   - Line 152: `style="flex:1; min-width:0; position:relative; z-index:1;"` → `class="tb-reveal-hit-card__content"`
   - Line 153 kicker `<div>` → `class="tb-reveal-hit-card__kicker"`
   - Line 154 title `<div>` → `class="tb-reveal-hit-card__title"`
   - Line 155 sub `<div>` → `class="tb-reveal-hit-card__sub"`
   - Line 156 mono pct `<span>` → `class="tb-mono tb-reveal-hit-card__sub-pct"`
   - Line 172: `style="flex:1; min-width:0;"` (sender-card text block) → `class="tb-flex-grow"`
   - Line 173 sender-card row → `class="tb-flex-row--sm-gap"`
   - Line 175 SSR small label → `<span class="tb-sender-card__name-ssr">SSR</span>`
   - Line 202: `style="flex:1; min-width:0;"` (miss-card content) → `class="tb-flex-grow"`
   - Line 203 miss kicker → `class="tb-reveal-miss-card__kicker"`
   - Line 204 miss title → `class="tb-reveal-miss-card__title"`
   - Line 205 miss sub → `class="tb-reveal-miss-card__sub"`
   - Line 206 miss mono pct → `class="tb-mono tb-reveal-miss-card__sub-pct"`
3. Run `grep -c 'style="' templates/Users/dashboard.php` → must equal 0. (The `style="--cols: 4;"` in tb_tabbar element is a different file — out of scope here.)
4. Open `src/Controller/UsersController.php`:
   - In `dashboard()`'s `$this->set([...])` block (around line 159-169), remove the `'blocks' => $blocks,` line.
   - Also remove the `$blocks` computation block (lines 109-115) to avoid a dangling query.
   - Update the `@var` PHPDoc in `templates/Users/dashboard.php` line 9 to drop the `$blocks` reference (since it's no longer passed).
5. Open `08-CONTEXT.md` and add a new locked-decision section near the bottom (after the existing Phase 7 deferred color exception, around line 95):
   ```
   ### Locked Decision — Acknowledged sub-grid micro-offset (Phase 8 cleanup)

   - **Scope:** `.tb-dash-box__url` margin-top, `.tb-reveal-hit-card__title` margin-top, `.tb-reveal-miss-card__title` margin-top
   - **Value:** `margin-top: 3px` (1px off the 4-grid)
   - **Justification:** matches hi-fi Dashboard.jsx and RevealHit.jsx typographic optical baseline.
   - **Scope-of-override:** these 3 selectors only; new 3px values require fresh review.
   ```
6. Add a comment in `tamabox.css` near line 2136 (`.tb-message-row[data-state="unread"] .tb-message-row__head { background: #FBFCFD; }`):
   ```css
   /* #FBFCFD — Phase 7 deferred color exception, see 08-CONTEXT.md Locked Decisions */
   ```
7. Run `composer test 2>&1 | tail -20` → must end "OK", 0 failures.

## Files

- `templates/Users/dashboard.php` (modify — 17 inline styles → classes, drop `$blocks` PHPDoc)
- `src/Controller/UsersController.php` (modify — drop `$blocks` set + query)
- `08-CONTEXT.md` (modify — add sub-grid locked decision section)
- `webroot/css/tamabox.css` (modify — add cross-ref comment near #FBFCFD)

## Verifier

- (a) Hi-fi side-by-side: dashboard render unchanged (CSS class replacement produces identical computed styles for each inline rule).
- (b) `composer test` green: 203 → 203 tests (no count delta — refactor), 0 failures.
- (c) Requirements satisfied: D-21 inline styles removed; D-22 `$blocks` cleaned; D-23 #FBFCFD documented (already in CONTEXT, comment cross-ref added); D-24 sub-grid 3px documented.

## Risks

- One inline style is in tb_tabbar element (`style="--cols: 4;"`) which is a **custom property** assignment, not a style — out of scope per UI-SPEC.
- Dropping `$blocks` from the controller MIGHT break a test if any test asserts on it. Pre-check: `grep -rn 'blocks' tests/TestCase/Controller/UsersControllerTest.php` — current dashboard tests assert on the block-list section (testDashboardRendersBlockListSection has been moved to `/dashboard/settings` per Phase 7 D-04). Settings page still queries `$blocks` separately — not affected.
- Class name typos cause visual regressions. Verify by post-deploy smoke (manual checklist item in 08-VERIFICATION.md).

## Out of scope

- Removing the `style="--cols: 4;"` CSS custom property declaration (it's a custom property, not styling)
- Touching `Inboxes/settings.php` (where `$blocks` is still legitimately consumed)
- Any other inline-style scrubs elsewhere in the codebase (only dashboard.php is in scope; other templates can be audited in v3 polish PR)
