---
phase: 8
plan: 08-08
slug: final-verification
status: ready
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05, 08-06, 08-07]
requirements: []
created: 2026-05-13
---

# Plan 08-08 — Final verification + v2 milestone close prep

## Goal

Write `08-VERIFICATION.md` (`status: human_needed`) — the final audit document that PASS/FAILs each of the 6 ROADMAP Phase 8 success criteria + the 4 Phase 7 cleanup carry-over items. Provide the smoke-checklist the user needs to run against `tamabox.emomie.com` after `git push lolipop main`. This is the last commit of Phase 8 / v2 milestone.

## Tasks

1. Re-read `08-CONTEXT.md` + `ROADMAP.md` §"Phase 8" + the 7 SUMMARY files (08-01..08-07) produced by the earlier plans.
2. Run `composer test 2>&1 | tail -20` one last time to capture the post-Phase-8 number for the report.
3. Write `08-VERIFICATION.md` with the structure described in the takeover brief:
   - Frontmatter: `phase: 8`, `slug: edge-microinteractions`, `status: human_needed`, `created: 2026-05-13`.
   - 6 sections (one per ROADMAP success criterion):
     1. SendNotFound 404 hi-fi 一致 (EDGE-01)
     2. SendInboxClosed hi-fi 一致 (EDGE-02)
     3. SendOverflow counter color + highlight + disabled CTA + chip (EDGE-03)
     4. SendFailed hi-fi 一致 (EDGE-04)
     5. Block confirm modal bottom sheet hi-fi 一致 (EDGE-05)
     6. `.tb-btn:active scale(0.985) 80ms` on all buttons (MOTION-01)
   - Each section PASS / FAIL based on (a) test results (b) markup audit (c) hi-fi side-by-side.
   - "Phase 7 cleanup carried out in Phase 8" section: 4 PASS rows (D-21, D-22, D-23, D-24).
   - "Smoke checklist for tamabox.emomie.com" section listing the manual UAT items (open a bad slug, toggle is_accepting, paste >2000 chars, click block from HIT message, press any tb-btn — each described with expected visual outcome).
4. (No code changes in this plan — pure documentation.)
5. Commit message `docs(08): verification report + v2 milestone close prep`.

## Files

- `08-VERIFICATION.md` (new)

## Verifier

- (a) Hi-fi side-by-side: documented inside the report (each section cites a hi-fi line range).
- (b) `composer test` green: final number captured in report (target 199 → 203+).
- (c) Requirements satisfied: all 6 ROADMAP success criteria audited; v2 milestone ready for `gsd-complete-milestone`.

## Risks

- A previous plan's commit may have left the test count at 199 (e.g., if the planned new test was made `markTestIncomplete`). Report the actual number and PASS/FAIL each plan's verifier accordingly.
- If any criterion is FAIL, the report explicitly states which plan needs follow-up before deploy. (Goal: all 6 PASS.)

## Out of scope

- Running the manual smoke against production (`git push lolipop main` + browser visit) — the user handles deploy
- Modifying any source file (this plan is documentation only)
- Updating `ROADMAP.md` Phase 8 checkbox / completed date (deferred to `gsd-complete-milestone`)
