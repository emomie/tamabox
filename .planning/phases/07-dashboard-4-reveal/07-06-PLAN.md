---
phase: 07-dashboard-4-reveal
plan: 06
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - templates/Users/discover.php
  - tests/TestCase/Controller/UsersControllerTest.php
autonomous: true
requirements: [NAV-04]

must_haves:
  truths:
    - "templates/Users/discover.php renders TbAppBar (title=発見, sub=箱をみつける) + search-input mock + tag chip row + Empty-state card + tb_tabbar footer (active='discover')"
    - "No DB queries; all content static"
    - "testDiscoverAuthRenders200 still passes; adds assertion that body contains '発見はもうすぐ来ます'"
    - "composer test 197+ tests / 0 failures"
  artifacts:
    - path: "templates/Users/discover.php"
      provides: "Discover tab Empty-state stub (NAV-04)"
      contains: "発見はもうすぐ来ます"
---

<objective>
Replace the placeholder `templates/Users/discover.php` (added in 07-02 Task 3) with the full Empty-state骨格 from `screens/Discover.jsx`. Static UI only — no DB, no JS interaction. The featured 箱 / list section from the full Discover.jsx is intentionally NOT shipped in v2 per CONTEXT.md D-16 / D-18 (DISC-01 v3 candidate).

Reference: 07-UI-SPEC.md Screen 2 (Discover).
</objective>

<context>
- /home/claude/projects/tamabox/.planning/phases/07-dashboard-4-reveal/07-UI-SPEC.md (Screen 2)
- /home/claude/projects/handoff_tamabox/screens/Discover.jsx (216 lines, Phase 7 takes Empty subset only)
- /home/claude/projects/tamabox/templates/Users/discover.php (placeholder from 07-02)
- /home/claude/projects/tamabox/webroot/css/tamabox.css §H.6 (Discover helpers from 07-01)
- /home/claude/projects/tamabox/tests/TestCase/Controller/UsersControllerTest.php (testDiscoverAuthRenders200 from 07-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write templates/Users/discover.php with Empty-state layout</name>
  <files>templates/Users/discover.php</files>
  <action>
Replace the placeholder content with:

```php
<?php
/**
 * @var \App\View\AppView $this
 * @var string $activeTab
 *
 * NAV-04 — 発見タブ Empty-state スタブ (Phase 7).
 * Hi-fi: ~/projects/handoff_tamabox/screens/Discover.jsx (Empty subset).
 *
 * No backend / no DB query. The full Discover feed (search / tag filter / featured 箱
 * / list) is deferred to v3 (DISC-01).
 */
$this->assign('title', '発見');
$tags = ['すべて', '創作', '音楽', '研究', '写真', 'ゲーム'];
?>
<div class="tb-dash-screen discover-page">
    <header class="tb-appbar tb-appbar--big">
        <div class="tb-appbar__left">
            <div>
                <div class="tb-appbar__title">発見</div>
                <div class="tb-appbar__sub">箱をみつける</div>
            </div>
        </div>
        <div class="tb-appbar__right"></div>
    </header>

    <div class="tb-dash-screen__body">
        <div class="tb-discover-search" aria-disabled="true" role="search">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--tb-ink-3)" stroke-width="1.8" stroke-linecap="round" aria-hidden="true">
                <circle cx="11" cy="11" r="7" />
                <path d="m20 20-3.5-3.5" />
            </svg>
            <span class="tb-discover-search__placeholder">@handle で箱をさがす</span>
        </div>

        <div class="tb-discover-tags" role="list" aria-label="カテゴリ">
            <?php foreach ($tags as $i => $tag): ?>
                <span class="tb-discover-tag<?= $i === 0 ? ' is-pseudo-active' : '' ?>" role="listitem" aria-disabled="true">
                    <?= h($tag) ?>
                </span>
            <?php endforeach; ?>
        </div>

        <div class="tb-empty-state">
            <div class="tb-empty-state__symbol" aria-hidden="true">✦</div>
            <div class="tb-empty-state__title">発見はもうすぐ来ます</div>
            <p class="tb-empty-state__body">他の人の箱を探して送信する機能は、近日公開予定です。今は自分の URL を直接共有してみてください。</p>
        </div>
    </div>

    <?= $this->element('tb_tabbar', ['active' => 'discover', 'unreadCount' => 0]) ?>
</div>
```
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; php -l templates/Users/discover.php &amp;&amp; grep -q "発見はもうすぐ来ます" templates/Users/discover.php &amp;&amp; grep -q "tb-tabbar" templates/Users/discover.php &amp;&amp; grep -q "tb-discover-search" templates/Users/discover.php &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains TbAppBar + search mock + tag chips + Empty-state card
    - Contains "発見はもうすぐ来ます" copy
    - Invokes tb_tabbar element with active=discover
    - php -l passes
  </acceptance_criteria>
  <done>Discover template ready.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen testDiscoverAuthRenders200 to assert body copy</name>
  <files>tests/TestCase/Controller/UsersControllerTest.php</files>
  <action>
Update `testDiscoverAuthRenders200`: add `$this->assertResponseContains('発見はもうすぐ来ます');` after the existing `assertResponseOk()`.
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; grep -q "発見はもうすぐ来ます" tests/TestCase/Controller/UsersControllerTest.php &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts body contains the Empty-state heading
  </acceptance_criteria>
  <done>Test strengthened (NEVER weakened — additive assertion).</done>
</task>

<task type="auto">
  <name>Task 3: Run composer test</name>
  <files></files>
  <action>
`cd ~/projects/tamabox && composer test 2>&1 | tail -10`. 197+ tests / 0 failures.
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; composer test 2>&1 | tail -3 | grep -qE "Tests: 19[7-9]" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - composer test reports 197+ tests, 0 failures
  </acceptance_criteria>
  <done>Test suite green.</done>
</task>

</tasks>

<verifier>

## (a) Hi-fi side-by-side check
- Open `~/projects/handoff_tamabox/screens/Discover.jsx` lines 30-65 (header + search + tags). Match the appbar / search mock / tag row visual.
- The Empty-state card is Phase 7-specific (Discover.jsx doesn't have one — it has featured + list); this is the locked v2 substitution per CONTEXT.md D-16.

## (b) composer test
197+ tests / 0 failures.

## (c) Controller behavior unchanged
- `UsersController::discover()` from 07-02 unchanged.
- No new DB queries.

## (d) Requirement satisfied
NAV-04 (Discover tab Empty-state hi-fi 骨格).

</verifier>

<risks>
- The inline magnifier SVG is not in the `icon` element registry. Acceptable per the SVG's locality — only used here in Phase 7. If a second use appears, lift to icon.php in a later phase.
- "発見はもうすぐ来ます" copy is a v2-internal choice — no v1 string to preserve.
</risks>

<out_of_scope>
- Real search backend (DISC-01 v3)
- Tag filter logic (v3)
- Featured 箱 card (v3, per D-16)
- Box list (v3)
</out_of_scope>
