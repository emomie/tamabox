---
phase: 07-dashboard-4-reveal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/element/tb_tabbar.php
  - webroot/css/tamabox.css
autonomous: true
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "templates/element/tb_tabbar.php exists and renders the .tb-tabbar markup with 4 anchor items"
    - "Element accepts `active` (string, one of inbox|discover|notifications|settings) and `unreadCount` (int)"
    - "Active item has `is-active` class + `aria-current=\"page\"`"
    - "Unread dot renders only when unreadCount > 0, anchored inside the inbox tab icon wrapper"
    - "tamabox.css has a new §H section with .is-opening fade-in keyframe + dashboard helpers"
    - "composer test still 195+ tests / 0 failures (no template consumes the element yet — pure addition)"
  artifacts:
    - path: "templates/element/tb_tabbar.php"
      provides: "Reusable footer TabBar for the 4 dashboard tabs"
      contains: ".tb-tabbar"
    - path: "webroot/css/tamabox.css"
      provides: "Phase 7 §H CSS helpers (.is-opening keyframe + dash layout helpers)"
      contains: "@keyframes tb-fade-in"
  key_links:
    - from: "(future) templates/Users/dashboard.php"
      to: "templates/element/tb_tabbar.php"
      via: "$this->element('tb_tabbar', ['active' => 'inbox', 'unreadCount' => $unreadCount])"
      pattern: "tb_tabbar"
---

<objective>
Create the `tb_tabbar` PHP element that 4 templates will consume, and append the Phase 7 §H CSS section to `webroot/css/tamabox.css` with the `.is-opening` keyframe (MOTION-02) plus the dashboard layout helpers used by subsequent plans.

This plan is intentionally **pure additive** — no existing template references `tb_tabbar` yet, so it cannot break any test. Subsequent plans (07-03, 07-05, 07-06, 07-07) consume the element.

Reference: 07-UI-SPEC.md Component 1 (TabBar) + CSS additions section.
</objective>

<context>
- /home/claude/projects/tamabox/.planning/phases/07-dashboard-4-reveal/07-CONTEXT.md
- /home/claude/projects/tamabox/.planning/phases/07-dashboard-4-reveal/07-UI-SPEC.md
- /home/claude/projects/handoff_tamabox/components.jsx (TbTabBar lines 116-125)
- /home/claude/projects/handoff_tamabox/screens/Dashboard.jsx (lines 90-98 for active=0 example)
- /home/claude/projects/tamabox/webroot/css/tokens.css (lines 207-230, .tb-tabbar base styles)
- /home/claude/projects/tamabox/webroot/css/tamabox.css (lines 1086-1102, .tb-unread-dot positioning)
- /home/claude/projects/tamabox/templates/element/icon.php (inbox / compass / bell / user SVGs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create templates/element/tb_tabbar.php</name>
  <files>templates/element/tb_tabbar.php</files>
  <action>
Create the file with the following structure (PHP shorthand, all output escaped):

- File-level docblock describes element parameters: `$active`, `$unreadCount` (with defaults).
- Cast `$active` to string and validate against the 4-tab whitelist; fall back to `''` if unknown.
- Cast `$unreadCount` to non-negative integer; default `0` if not set.
- Define an array `$items` containing 4 entries with keys `id`, `href`, `iconName`, `label` (受信/発見/通知/設定).
- Emit a `<nav class="tb-tabbar" role="tablist" aria-label="ダッシュボードタブ" style="--cols: 4;">` wrapper.
- For each item, emit an `<a>`:
  - `class="tb-tabbar__item"` + `is-active` when `id === active`
  - `href` from item def
  - `aria-current="page"` only on active item
  - `role="tab"`
  - Inner: `<span class="tb-tabbar__icon">` containing the `icon` element AND, for the inbox item only, an `<span class="tb-unread-dot">` when `$unreadCount > 0` with an `aria-label="未読 N 件"`.
  - Outer label `<span><?= label ?></span>` AFTER the icon wrapper.
- The `<a>` href values: `/dashboard`, `/dashboard/discover`, `/dashboard/notifications`, `/dashboard/settings`.

Do NOT add unrelated CSS — that lives in Task 2. The element is structure-only.
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; php -l templates/element/tb_tabbar.php &amp;&amp; grep -q "tb-tabbar" templates/element/tb_tabbar.php &amp;&amp; grep -q "aria-current" templates/element/tb_tabbar.php &amp;&amp; grep -q "tb-unread-dot" templates/element/tb_tabbar.php &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists, `php -l` reports no syntax error
    - Contains 4 `<a>` items with hrefs /dashboard, /dashboard/discover, /dashboard/notifications, /dashboard/settings
    - Contains `aria-current` (active item)
    - Contains `tb-unread-dot` (only rendered when count > 0)
    - Contains `icon` element invocations for inbox / compass / bell / user
    - No PHP notices when rendered with `active='inbox'`, `unreadCount=0` (covered later by smoke)
  </acceptance_criteria>
  <done>tb_tabbar element exists; ready to be consumed by Phase 7 templates.</done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 7 §H CSS section to webroot/css/tamabox.css</name>
  <files>webroot/css/tamabox.css</files>
  <action>
Append the full Phase 7 §H section as documented in 07-UI-SPEC.md. The block opens with a banner comment:

```css
/* ============================================================
 * Phase 7 — Dashboard 4-tab + Reveal motion
 * Source: .planning/phases/07-dashboard-4-reveal/07-UI-SPEC.md
 * ============================================================ */
```

Then the sub-sections (§H.1 through §H.7) verbatim from UI-SPEC. In particular:
- §H.1 TabBar internal tweaks (text-decoration + focus-visible)
- §H.2 Reveal fade-in keyframe `tb-fade-in` + `.message-row__body.is-opening { animation: tb-fade-in 400ms ease; }`
- §H.3 `.tb-dash-box*` / `.tb-dash-counts*` helpers
- §H.4 `.tb-receive-list` / `.tb-message-row*` / `.tb-dash-dot*`
- §H.5 `.tb-reveal-miss-card*` / `.tb-reveal-hit-card*` / `.tb-sender-card*`
- §H.6 `.tb-discover-*` / `.tb-empty-state*` / `.tb-notif-empty*`
- §H.7 `.tb-dash-screen*` wrapper

All values use `var(--tb-*)` tokens. No raw hex values except the 3 explicitly approved in UI-SPEC (`#FBFCFD` unread row tint, `#FFF7E0`/`#FBEFCC` warm gradient, `#F0DCA8` warm border, `#E7C795`/`#B98449` avatar gradient — these are hi-fi-verbatim per D-22/D-23).
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; grep -q "Phase 7 — Dashboard 4-tab" webroot/css/tamabox.css &amp;&amp; grep -q "@keyframes tb-fade-in" webroot/css/tamabox.css &amp;&amp; grep -q "tb-message-row" webroot/css/tamabox.css &amp;&amp; grep -q "tb-empty-state" webroot/css/tamabox.css &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains Phase 7 banner comment
    - `@keyframes tb-fade-in` defined
    - All §H sub-sections present
    - No CSS syntax errors (no stray `*/` inside variable names — beware the `--color-*/...` trap)
  </acceptance_criteria>
  <done>tamabox.css has Phase 7 §H section; consumed by Tasks 3-7 of subsequent plans.</done>
</task>

<task type="auto">
  <name>Task 3: Run composer test to confirm green baseline</name>
  <files></files>
  <action>
Run `cd ~/projects/tamabox && composer test 2>&1 | tail -10`. Must end with "OK" (or "OK, but incomplete...") and 195+ tests, 0 failures.

This plan adds files but consumes them nowhere; no test should change.
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; composer test 2>&1 | tail -3 | grep -qE "OK|Tests: 19[5-9]" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - composer test reports 195+ tests, 0 failures
  </acceptance_criteria>
  <done>Test suite still green.</done>
</task>

</tasks>

<verifier>

## (a) Hi-fi side-by-side check
Open `~/projects/handoff_tamabox/components.jsx` lines 116-125 (TbTabBar React) and `~/projects/handoff_tamabox/screens/Dashboard.jsx` lines 90-98 (4-item invocation). Confirm:
- Outer `<nav class="tb-tabbar">` corresponds to React `<nav className="tb-tabbar">`
- 4 items: inbox / compass / bell / user
- Active item gets `is-active` (matches `i === active` ternary)

## (b) composer test
`cd ~/projects/tamabox && composer test 2>&1 | tail -5` must end with "OK" and 195+ tests, 0 failures.

## (c) Controller behavior unchanged
No `src/` file touched. No controller signature changed.

## (d) Requirement satisfied
NAV-01 (4 tabs structurally present in the new element — wiring to controllers happens in 07-02/03/05/06/07) and NAV-02 (CSS for is-active + unread dot present) — element ready for consumption.

</verifier>

<risks>
- The element is hand-rolled (not auto-generated from `components.jsx`); minor whitespace differences are fine. The DOM contract is the authoritative output spec.
- Adding 150+ lines of CSS in §H may push `tamabox.css` over 2200 lines. Acceptable — Phase 5/6 already established this growth.
</risks>

<out_of_scope>
- Wiring the element from any template (handled in 07-03, 07-05, 07-06, 07-07)
- The `reveal-motion.js` file (handled in 07-04)
- Controller actions / routes (handled in 07-02)
</out_of_scope>
