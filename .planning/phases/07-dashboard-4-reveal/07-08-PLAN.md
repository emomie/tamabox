---
phase: 07-dashboard-4-reveal
plan: 08
type: verify
wave: 4
depends_on: ["07-03", "07-04", "07-05", "07-06", "07-07"]
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "All Phase 7 files in place: tb_tabbar element, dashboard / discover / notifications / settings templates, reveal-motion.js, §H CSS"
    - "composer test 197+ tests / 0 failures"
    - "4 tabs reachable via direct URL: /dashboard, /dashboard/discover, /dashboard/notifications, /dashboard/settings"
    - "Manual smoke checklist generated for post-deploy human validation"
---

<objective>
Final verification of Phase 7. No code edits — this plan runs the test suite, audits file presence, generates the manual smoke checklist, and (optionally) flags any cleanup items for follow-up.
</objective>

<context>
- All preceding plan artifacts
- /home/claude/projects/tamabox/.planning/phases/07-dashboard-4-reveal/07-UI-SPEC.md "Verification Checklist"
</context>

<tasks>

<task type="auto">
  <name>Task 1: File-presence audit</name>
  <files></files>
  <action>
Run these checks and confirm each file exists and matches expected size range:
```sh
ls -l templates/element/tb_tabbar.php
ls -l templates/Users/dashboard.php templates/Users/discover.php templates/Users/notifications.php
ls -l templates/Inboxes/settings.php
ls -l webroot/js/reveal-motion.js
grep -c "Phase 7 — Dashboard 4-tab" webroot/css/tamabox.css
```
Expected:
- tb_tabbar.php exists (≥30 lines)
- dashboard.php ~150-220 lines
- discover.php ~40-80 lines
- notifications.php ~25-50 lines
- settings.php ~25-50 lines
- reveal-motion.js ~25-40 lines
- tamabox.css contains Phase 7 banner (1 match)
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; ls templates/element/tb_tabbar.php templates/Users/dashboard.php templates/Users/discover.php templates/Users/notifications.php templates/Inboxes/settings.php webroot/js/reveal-motion.js &amp;&amp; grep -q "Phase 7 — Dashboard 4-tab" webroot/css/tamabox.css &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All Phase 7 files present
    - CSS Phase 7 banner present
  </acceptance_criteria>
  <done>File presence verified.</done>
</task>

<task type="auto">
  <name>Task 2: Test suite final run</name>
  <files></files>
  <action>
`cd ~/projects/tamabox && composer test 2>&1 | tail -10`. Verify 197+ tests / 0 failures.
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; composer test 2>&1 | tail -3 | grep -qE "Tests: 19[7-9]|Tests: 20[0-5]" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - composer test reports 197+ tests, 0 failures
  </acceptance_criteria>
  <done>Test suite final-state verified.</done>
</task>

<task type="auto">
  <name>Task 3: Substring-preservation audit (D-19 / D-20 compliance)</name>
  <files></files>
  <action>
Confirm all Phase 4 assertion substrings are still emitted by Phase 7 dashboard.php:
- `data-state="unread"` and `data-state="opened"`
- `★ 抽選 hit`
- `★ 抽選 miss`
- `https://bsky.app/profile/`
- `rel="noopener"`
- `action="/dashboard/messages/{id}/open"`
- `開封する`
- `ようこそ`
- `そのページはありません`
- `まだ受信したメッセージはありません`

```sh
for s in 'data-state="unread"' 'data-state="opened"' '★ 抽選 hit' '★ 抽選 miss' \
         'https://bsky.app/profile/' 'rel="noopener"' '開封する' 'ようこそ' \
         'そのページはありません' 'まだ受信したメッセージはありません'; do
  grep -qF "$s" templates/Users/dashboard.php || echo "MISSING: $s"
done
```
Expected: no MISSING output.
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; for s in 'data-state="unread"' '★ 抽選 hit' '★ 抽選 miss' 'https://bsky.app/profile/' 'rel="noopener"' '開封する' 'ようこそ'; do grep -qF "$s" templates/Users/dashboard.php || (echo MISSING: $s; exit 1); done &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All Phase 4 assertion substrings still present in dashboard.php
  </acceptance_criteria>
  <done>Test-substring preservation audited.</done>
</task>

<task type="auto">
  <name>Task 4: Backend-immutability audit (D-19 / D-20 boundary check)</name>
  <files></files>
  <action>
Confirm only the allowed src/ + config/ files were modified compared to baseline (Phase 6 smoke commit 5385ede):

```sh
git diff --stat 5385ede..HEAD -- src/ config/
```

Expected files in diff:
- `src/Controller/UsersController.php` (discover + notifications + unreadCount additions)
- `src/Controller/InboxesController.php` (GET render branch — added in 07-03 Task 3)
- `config/routes.php` (2 routes added)

NO other src/ file modified. NO `config/Migrations/` file modified.
  </action>
  <verify>
    <automated>cd ~/projects/tamabox &amp;&amp; git diff --name-only 5385ede..HEAD -- src/ config/ | sort | tr '\n' ' ' | grep -qE "^config/routes\.php src/Controller/InboxesController\.php src/Controller/UsersController\.php $" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Only the 3 allowed files appear in the src/+config/ diff
  </acceptance_criteria>
  <done>Backend immutability verified.</done>
</task>

<task type="auto">
  <name>Task 5: Generate manual smoke checklist</name>
  <files></files>
  <action>
The manual smoke checklist for human validation post-deploy (recorded in 07-VERIFICATION.md):

1. Log in at https://tamabox.emomie.com — confirm dashboard loads and 受信箱 AppBar renders
2. Confirm TabBar visible at footer with 4 items, 受信 highlighted in turquoise
3. Click 発見 → /dashboard/discover renders Empty state "発見はもうすぐ来ます"
4. Click 通知 → /dashboard/notifications renders Empty state "通知はまだありません"
5. Click 設定 → /dashboard/settings renders form + block list, 設定 tab highlighted
6. Back to 受信 → click an unread message → body fades in (visual 400ms ease)
7. Open an SSR hit message → warm gradient lottery card + sender card with プロフィール pill rendered
8. Open an SSR miss message → quiet dashed-circle MISS card rendered
9. Save settings (e.g. change SSR % to 5%) → 302 redirect to /dashboard with flash "保存しました"
10. Unread count badge (未開封 N) renders on Counts row when count > 0

Output this checklist into a markdown file. Plan 07-VERIFICATION.md (created in next step, not by this task) will reproduce these items.
  </action>
  <verify>
    <automated>echo "Manual smoke checklist documented (consumed by 07-VERIFICATION.md)" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Checklist articulated
  </acceptance_criteria>
  <done>Smoke checklist prepared for VERIFICATION.md.</done>
</task>

</tasks>

<verifier>

## (a) Hi-fi side-by-side check
Phase 8's review covers all 6 hi-fi side-by-side audits per ROADMAP Phase 7 success criterion. See 07-VERIFICATION.md.

## (b) composer test
197+ tests / 0 failures — confirmed in Task 2.

## (c) Controller behavior unchanged
- UsersController::dashboard() POST/GET behavior preserved; new view variables additive (Task 4 confirmed via git diff)
- InboxesController::settings() POST unchanged; GET expanded from 302 to render (D-19 allowance)
- UsersController::discover()/notifications() are NEW actions (D-19 allowance)
- routes.php 2 new GET-only routes (D-19 allowance)
- No model/migration touched (Task 4 confirms)

## (d) Requirement satisfied
- NAV-01 (Task 1: tb_tabbar in 4 templates) ✓
- NAV-02 (Task 1: is-active + unread dot in element) ✓
- NAV-03 (Task 1: dashboard.php is hi-fi Dashboard.jsx) ✓
- NAV-04 (Task 1: discover.php hi-fi Empty state) ✓
- NAV-05 (Task 1: notifications.php hi-fi Empty state) ✓
- NAV-06 (Task 1: settings.php hi-fi Settings.jsx layout) ✓
- MOTION-02 (Task 1: reveal-motion.js + §H.2 keyframe) ✓
- MOTION-03 (Task 1: RevealHit sender card in dashboard.php) ✓

</verifier>

<risks>
- Manual smoke depends on deploy; this plan only confirms code shape. The user runs the deploy + Discord-verifies the 10-item checklist.
- Some pagination or fixture-edge scenarios may not be exercised by the test suite; rely on staging smoke for those.
</risks>

<out_of_scope>
- Lolipop deploy (user handles `git push lolipop main`)
- Real notification feed (NOTIF-01 v3)
- Real discover feed (DISC-01 v3)
- 3D rotateX封筒 (v3 MOTION-X1)
</out_of_scope>
